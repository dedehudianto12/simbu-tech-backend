// Middleware verifikasi JWT untuk route /api/admin/*.
// Endpoint publik (/api/public/*) TIDAK pakai middleware ini — sesuai keputusan
// "tidak perlu login untuk lihat status, cukup nomor tiket".
package middleware
